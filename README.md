# Tasklogger

*app containing list of tasks completed for a user with bad memory*

## Contains:
1. Listview containing a list of tasks completed
2. Tasks fo be written into list when a button is pressed
3. Tasks to be on the list after we close the app